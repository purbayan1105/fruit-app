const Note = () => {
  return (
    <div className="h-[10dvh] flex justify-center items-center text-gray-400 poppins text-xs text-center md:text-sm ">
      This website is built using Next JS and product data is stored in MongoDB
      and fetched from the database with react-query
    </div>
  );
};

export default Note;
